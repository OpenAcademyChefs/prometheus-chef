#!/bin/bash

start(){

GOMAXPROCS=<%= @gomaxproc %> <%= @path %> <%= @flag %> &
}

stop(){
	PID=$(pidof prometheus)
	kill $PID
}

restart(){
	stop
	start
}

case "$1" in
  start)
        start
        ;;
  stop)
        stop
        ;;
   restart)
   		restart
   		;;
*)

esac
exit 0
